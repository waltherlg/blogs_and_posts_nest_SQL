import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { newPasswordSetInput} from './../../auth.controller';
import { UsersRepository } from '../../../users/users.repository';


export class NewPasswordSetCommand{
    constructor(public newPasswordDto: newPasswordSetInput){}
}

@CommandHandler(NewPasswordSetCommand)

export class NewPasswordSetUseCase implements ICommandHandler<NewPasswordSetCommand>{
    constructor(private readonly usersRepository: UsersRepository){}
    async execute(command: NewPasswordSetCommand): Promise<any> {
        
    }
}